@AGENTS.md

Sygnal’s agent map, conventions, and “where truth lives” are in `AGENTS.md`. Architecture diagrams: `docs/ARCHITECTURE.md`. Do not duplicate create-next-app boilerplate here. The Next.js 16 notice at the top of `AGENTS.md` is required (`next dev` re-inserts it if removed).
